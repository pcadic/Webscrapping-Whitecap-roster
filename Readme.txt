The script scrapes some content that is summarized dynamically and presented in a graph (Whitecaps website site used as an example). 
Chrome and Chromedriver should be installed (Update Path in the script)
Packages used : time, webdriver from selenium, BeautifulSoup from bs4, pandas and matplotlib.pyplot